function printCharacters(word) {
    for (let letter of word) {
        console.log(letter);
    }
}
printCharacters('AWord');
printCharacters('Sentence');

// let printChars = (word) => word.forEach((x) => console.log(x));
// printChars('Aword');